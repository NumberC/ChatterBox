<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
</head>
<body>
    <h1>{{name}}</h1> 
    <h3>{{quote}}</h3>
    <p>{{description}}</p>
    <input type="text" id="query">
    <button id="submit" onclick="search()">Search</button>

    <script>
        function search(){
            window.location = "/users/" + document.getElementById("query").value;
        }
    </script>
</body>
</html>